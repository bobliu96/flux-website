apiVersion: v1
kind: Service
metadata:
  name: exam-q6
spec:
  type: NodePort
  selector:
    app: exam-q6
  ports:
  - port: 80
    nodePort: 30080
    protocol: TCP
    targetPort: 80
  selector:
    app: exam-q6